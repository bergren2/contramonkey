<!DOCTYPE html>
<html>
  <head>
    <title>{{@blog.title}}</title>

    <link rel="stylesheet" type="text/css" href="{{asset "css/index.css"}}" />
  </head>
  <body>
    <header {{#if @blog.cover}} style="background:
      url({{@blog.cover}}){{else}}no-cover{{/if}} 0/cover no-repeat;">
      <h1>{{@blog.title}}</h1>
    </header>

    <nav>
    </nav>

    {{{body}}}

    <footer>
    </footer>
  </body>
</html>
